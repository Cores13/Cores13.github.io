import React, { ReactElement } from 'react';
import './HighSchool.css';

interface Props {
    
}

export default function HighSchool({}: Props): ReactElement {
    return (
        <div className="highSchool">
            HEYY
        </div>
    )
}
